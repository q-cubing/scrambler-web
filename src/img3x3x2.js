function i3x3x2(c, colors) {
    c.fillStyle = colors.a;  c.fillRect(90, 15, 25, 25);  // A
    c.fillStyle = colors.ae; c.fillRect(120, 15, 25, 25); // Ae
    c.fillStyle = colors.b;  c.fillRect(150, 15, 25, 25); // B
    c.fillStyle = colors.de; c.fillRect(90, 45, 25, 25);  // De
    c.fillStyle = colors.uc; c.fillRect(120, 45, 25, 25); // uc
    c.fillStyle = colors.be; c.fillRect(150, 45, 25, 25); // Be
    c.fillStyle = colors.d;  c.fillRect(90, 75, 25, 25);  // D
    c.fillStyle = colors.ce; c.fillRect(120, 75, 25, 25); // Ce
    c.fillStyle = colors.c;  c.fillRect(150, 75, 25, 25); // C

    c.fillStyle = colors.e;  c.fillRect(0, 105, 25, 25);  // E
    c.fillStyle = colors.ee; c.fillRect(30, 105, 25, 25); // Ee
    c.fillStyle = colors.f;  c.fillRect(60, 105, 25, 25); // F
    c.fillStyle = colors.h;  c.fillRect(0, 135, 25, 25);  // H
    c.fillStyle = colors.fe; c.fillRect(30, 135, 25, 25); // Fe
    c.fillStyle = colors.g;  c.fillRect(60, 135, 25, 25); // G

    c.fillStyle = colors.i;  c.fillRect(90, 105, 25, 25);  // I
    c.fillStyle = colors.ge; c.fillRect(120, 105, 25, 25); // Ge
    c.fillStyle = colors.j;  c.fillRect(150, 105, 25, 25); // J
    c.fillStyle = colors.l;  c.fillRect(90, 135, 25, 25);  // L
    c.fillStyle = colors.he; c.fillRect(120, 135, 25, 25); // He
    c.fillStyle = colors.k;  c.fillRect(150, 135, 25, 25); // K

    c.fillStyle = colors.m;  c.fillRect(180, 105, 25, 25); // M
    c.fillStyle = colors.ie; c.fillRect(210, 105, 25, 25); // Ie
    c.fillStyle = colors.n;  c.fillRect(240, 105, 25, 25); // N
    c.fillStyle = colors.p;  c.fillRect(180, 135, 25, 25); // P
    c.fillStyle = colors.je; c.fillRect(210, 135, 25, 25); // Je
    c.fillStyle = colors.o;  c.fillRect(240, 135, 25, 25); // O

    c.fillStyle = colors.q;  c.fillRect(270, 105, 25, 25); // Q
    c.fillStyle = colors.ke; c.fillRect(300, 105, 25, 25); // Ke
    c.fillStyle = colors.r;  c.fillRect(330, 105, 25, 25); // R
    c.fillStyle = colors.t;  c.fillRect(270, 135, 25, 25); // T
    c.fillStyle = colors.le; c.fillRect(300, 135, 25, 25); // Le
    c.fillStyle = colors.s;  c.fillRect(330, 135, 25, 25); // S

    c.fillStyle = colors.u;  c.fillRect(90, 165, 25, 25);  // U
    c.fillStyle = colors.me; c.fillRect(120, 165, 25, 25); // Me
    c.fillStyle = colors.v;  c.fillRect(150, 165, 25, 25); // V
    c.fillStyle = colors.pe; c.fillRect(90, 195, 25, 25);  // Pe
    c.fillStyle = colors.dc; c.fillRect(120, 195, 25, 25); // dc
    c.fillStyle = colors.ne; c.fillRect(150, 195, 25, 25); // Ne
    c.fillStyle = colors.x;  c.fillRect(90, 225, 25, 25);  // X
    c.fillStyle = colors.oe; c.fillRect(120, 225, 25, 25); // Oe
    c.fillStyle = colors.w;  c.fillRect(150, 225, 25, 25); // W
}
