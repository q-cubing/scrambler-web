function i2x2(c, colors) {
    c.fillStyle = colors.a; c.fillRect(90, 0, 40, 40);   // A
    c.fillStyle = colors.b; c.fillRect(135, 0, 40, 40);  // B
    c.fillStyle = colors.d; c.fillRect(90, 45, 40, 40);  // D
    c.fillStyle = colors.c; c.fillRect(135, 45, 40, 40); // C

    c.fillStyle = colors.e; c.fillRect(0, 90, 40, 40);   // E
    c.fillStyle = colors.f; c.fillRect(45, 90, 40, 40);  // F
    c.fillStyle = colors.h; c.fillRect(0, 135, 40, 40);  // H
    c.fillStyle = colors.g; c.fillRect(45, 135, 40, 40); // G

    c.fillStyle = colors.i; c.fillRect(90, 90, 40, 40);   // I
    c.fillStyle = colors.j; c.fillRect(135, 90, 40, 40);  // J
    c.fillStyle = colors.l; c.fillRect(90, 135, 40, 40);  // L
    c.fillStyle = colors.k; c.fillRect(135, 135, 40, 40); // K

    c.fillStyle = colors.m; c.fillRect(180, 90, 40, 40);  // M
    c.fillStyle = colors.n; c.fillRect(225, 90, 40, 40);  // N
    c.fillStyle = colors.p; c.fillRect(180, 135, 40, 40); // P
    c.fillStyle = colors.o; c.fillRect(225, 135, 40, 40); // O

    c.fillStyle = colors.q; c.fillRect(270, 90, 40, 40);  // Q
    c.fillStyle = colors.r; c.fillRect(315, 90, 40, 40);  // R
    c.fillStyle = colors.t; c.fillRect(270, 135, 40, 40); // T
    c.fillStyle = colors.s; c.fillRect(315, 135, 40, 40); // S

    c.fillStyle = colors.u; c.fillRect(90, 180, 40, 40);  // U
    c.fillStyle = colors.v; c.fillRect(135, 180, 40, 40); // V
    c.fillStyle = colors.x; c.fillRect(90, 225, 40, 40);  // X
    c.fillStyle = colors.w; c.fillRect(135, 225, 40, 40); // W
}
